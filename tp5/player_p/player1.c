#include "actions.h"
#include <stdio.h>

#define MAX_PLAY_ACTION 20

char play_actions[] = {
    ACTION_TELEPORT_D,    
};

char get_action()
{
    char ret_val = ACTION_MOVE_R;

    return (ret_val);
}

void player_init(t_player* p_player, uint8_t num)
{
    
}